version: "3"
services:
  breastfeedtgbot:
    #Build using local Dockerfile
    environment:
      TZ: Europe/Helsinki
    build: .
    #Link to the tg container
    ports:
      - "5005:5005"
    restart: "unless-stopped"
